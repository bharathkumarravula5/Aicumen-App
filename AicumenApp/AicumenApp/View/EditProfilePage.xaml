<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="AicumenApp.View.EditProfilePage"
            Title="Edit Profile">
    <ContentPage.Content>
        <StackLayout Margin="30,0,30,0">
            <Entry Text="{Binding fb_name}" FontSize="Default"/>
            <Entry Text="{Binding fb_id}" FontSize="Default"/>
            <Entry Text="{Binding fb_gender}" FontSize="Default"/>
            <Entry Text="{Binding fb_email}" FontSize="Default"/>
            <Entry Text="{Binding stq_score}" FontSize="Default"/>
            <Entry Text="{Binding qrm_wallet}" FontSize="Default"/>

            <Button Text="Update Profile" Margin="30,0,30,0" Command="{Binding UpdateProfile}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>